# Context

React Context para estado compartido del feature, solo si aporta valor.

En el MVP el estado del recorder se gestiona con hooks y props. Añadir contexto aquí si más adelante varios componentes necesitan acceso al mismo estado de grabación sin prop drilling.
