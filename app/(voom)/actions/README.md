# Actions

Server Actions o llamadas al backend / mutaciones del feature voom.

En el MVP la descarga es 100% client-side. Aquí se pueden añadir, por ejemplo:

- Subir la grabación a un almacenamiento (S3, etc.)
- Guardar metadatos en base de datos
- Procesamiento en servidor del video
